# Secrets — never commit
.env
.env.local
*.pem
*.key

# Python
.venv/
__pycache__/
*.py[cod]
.pytest_cache/

# Node / Next.js
node_modules/
.next/
out/
*.tsbuildinfo
.vercel

build/

# Deployment and run evidence is committed on purpose: it is the proof that the
# contract is live and the lifecycle really executed. It holds no secrets.
artifacts/fee-profile.json
.DS_Store
.env*

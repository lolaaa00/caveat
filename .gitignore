# Secrets — never commit
.env
.env.local
*.pem
*.key

# Python
.venv/
__pycache__/
*.py[cod]
.pytest_cache/

# Node / Next.js
node_modules/
.next/
out/
*.tsbuildinfo
.vercel

build/

# Deployment and run evidence (including artifacts/fee-profile.json) is committed on
# purpose: it is the proof that the contract is live and the lifecycle really executed.
# It holds no secrets.
.DS_Store
.env*
